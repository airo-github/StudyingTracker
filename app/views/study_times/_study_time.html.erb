<div class="p-1 lg:w-1/3 md:w-1/2 w-full">
  <div class="h-full flex items-center border-accent-content border p-4 rounded-lg ">
    <div class="avatar">
      <div class="w-16 h-16 object-cover object-center flex-shrink-0 rounded-full mr-4">
        <%= image_tag study_time.user.avatar.url %>
      </div>
    </div>
    <div class="flex-grow text-lg font-top">
      <%= study_time.user.name %>
      <p>開始時間 <%= l study_time.created_at, format: :long %></p>
      <% if study_time.status == 0 %>
        <p>更新時間 <%= l study_time.updated_at, format: :long %></p>
      <% else %>
        <p>終了時間 <%= l study_time.updated_at, format: :long %></p>
      <% end %>
      <p>合計時間 <%= "#{study_time.total_time / 60}時間#{study_time.total_time % 60}分" %></p>
      <div class="flex items-center flex-wrap justify-end">
        <% if current_user == study_time.user && study_time.status == 0 %>
          <%= link_to '共有', "https://twitter.com/share?url=https://studying-tracker.fly.dev/&text=#{CGI.escape("#{study_time.user.name}さんの現在の学習時間は#{study_time.total_time / 60}時間#{study_time.total_time % 60}分です！")}", class: 'btn btn-info fa-brands fa-twitter fa-2xl text-twitter', target: '_blank' %>
          <%= form_with(model: study_time, url: study_time_path(study_time)) do |f| %>
            <%= f.submit "更新", class: "btn btn-primary text-headfoot" %>
          <% end %>
          <%= form_with(model: study_time, url: finish_study_time_path(study_time)) do |f| %>
            <%= f.submit "終了", class: "btn btn-secondary text-headfoot" %>
          <% end %>
        <% end %>
        <% if current_user == study_time.user && study_time.status == 1 %>
          <%= link_to '共有', "https://twitter.com/share?url=https://studying-tracker.fly.dev/&text=#{CGI.escape("#{study_time.user.name}さんの今回の学習時間は#{study_time.total_time / 60}時間#{study_time.total_time % 60}分でした！")}", class: 'btn btn-info fa-brands fa-twitter fa-2xl text-twitter', target: '_blank' %>
          <%= button_to t('study_times.destroy'), study_time_path(study_time), class: "btn btn-secondary", method: :delete, "data-confirm" => t('defaults.message.destroy_confirm') %>
        <% end %>
        </div>
    </div>
  </div>
</div>
